# `cli/` is the one TypeScript pnpm workspace package. `landing/` is a vault
# content directory (markdown, images), not a node project; adding it here
# would force pnpm to create unwanted node_modules and tsconfig.references.
packages:
  - cli
