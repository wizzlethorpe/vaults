
# Vaults

- vaults preview for pages that only contain base code is ugly (shows base code instead of rendered content).
- I feel like we should make vaults a monorepo instead of having a separate repo for the landing, foundry, module and CLI. That way when I push releases they can all be consistent with each other and even use the same version number. What do you think?


# WANDS
