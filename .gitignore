# Generic
node_modules/
.DS_Store
*.log
.env

# Build artifacts
cli/dist/
cli/*.tsbuildinfo
ttrpg/dist/
ttrpg/*.tsbuildinfo

# Wrangler / Cloudflare local state
.wrangler/

# Per-vault internal state (vaults-cli writes config + cache here)
**/.vaults/cache/
**/.vaults/config.json
.vault-cache/
