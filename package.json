{
  "name": "vaults-monorepo",
  "private": true,
  "description": "Monorepo: the vaults CLI and the landing-site vault.",
  "scripts": {
    "typecheck": "pnpm -r exec tsc --noEmit && pnpm --filter @wizzlethorpe/vaults run typecheck:test",
    "test": "pnpm -r --if-present test",
    "build:cli": "pnpm --filter @wizzlethorpe/vaults run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.0.0"
}
