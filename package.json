{
  "name": "vaults-monorepo",
  "private": true,
  "description": "Monorepo: the vaults CLI, its TTRPG add-on, and the landing-site vault.",
  "scripts": {
    "typecheck": "pnpm -r exec tsc --noEmit && pnpm -r run typecheck:test",
    "test": "pnpm -r --if-present test",
    "build": "pnpm --filter @wizzlethorpe/vaults run build && pnpm --filter @wizzlethorpe/vaults-ttrpg run build"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@wizzlethorpe/vaults-ttrpg": "workspace:*"
  },
  "packageManager": "pnpm@9.0.0"
}
